# Copiez ce fichier vers .env pour la prod ou des surcharges locales :
#   cp .env.docker.dev .env
#
# .env.docker.dev est versionné (Docker dev). .env reste privé (gitignore).

ACCESS_CODE=364728
DB_HOST=localhost
DB_NAME=parking
DB_USER=parking
DB_PASSWORD=parking
DB_ROOT_PASSWORD=root
TZ=Europe/Paris
# Générez la vôtre en prod : php -r 'echo base64_encode(random_bytes(32)), PHP_EOL;'
ENCRYPTION_KEY=BsfFZZUmARcn0yQuLr205t0gD6EhSN9bzmhQjmxPK6M=
